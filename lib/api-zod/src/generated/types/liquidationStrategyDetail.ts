/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import type { LiquidationCompetitor } from './liquidationCompetitor';

/**
 * Real, verified deep execution readiness for one specific position — not a theoretical estimate. Every field is either read live on-chain/API or explicitly null when we couldn't check it, never guessed.
 */
export interface LiquidationStrategyDetail {
  /** Whether a real swap route (Uniswap V3 / Sushiswap V3 or V2 / Camelot V2) could be built for this exact trade size right now. Null if the check itself failed. */
  routeBuildable: boolean | null;
  /** Why the route isn't buildable, when routeBuildable is false (e.g. no pool on any supported venue has enough depth for this trade size). */
  routeBlockedReason: string | null;
  /** Real gas cost estimate in USD — live eth_estimateGas if an ArbExecutor is deployed here, otherwise the average of real recent historical liquidations on this exact market, priced at the current gas price. Null when not computed. */
  estimatedGasCostUsd: number | null;
  /** How estimatedGasCostUsd was derived, e.g. "live simulation" or "30d historical average (N liquidations)". */
  gasCostBasis: string | null;
  /** estimatedBonusUsd minus estimatedGasCostUsd, only meaningful when both are known. */
  estimatedNetProfitUsd: number | null;
  /** Real liquidators who have executed LiquidationCall transactions on this exact market in the last 30 days, most active first — who you're actually racing against here. */
  topCompetitors: LiquidationCompetitor[];
  /** How many historical LiquidationCall events on this market this detail was derived from (0 means no history was found — a market that has never seen a liquidation, or one just deployed). */
  sampleCount: number;
}
