/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ErrorResponse {
  error: string;
}

export interface ScannerSummary {
  activeOpportunities: number;
  poolsScanned: number;
  tokensTracked: number;
  estimatedNetProfit24h: number;
  lastScanAt: string;
  scanLatencyMs: number;
  /** Raw price/graph candidates found before route-closure and adapter checks. */
  candidatesDiscovered: number;
  /** Candidates whose complete same-chain route is supported by verified adapters. */
  routableCandidates: number;
  /** Complete routes that received an exact on-chain quote. */
  routesQuoted: number;
  /** Routable candidates whose exact quote failed or was unavailable. */
  routesUnavailable: number;
  /** Percentage of discovered candidates converted into complete routable cycles. */
  routeCoveragePct: number;
  /** Percentage of routable candidates with an exact quote in this scan. */
  quoteCoveragePct: number;
}

export type NetworkStatusStatus = typeof NetworkStatusStatus[keyof typeof NetworkStatusStatus];


export const NetworkStatusStatus = {
  healthy: 'healthy',
  degraded: 'degraded',
  unavailable: 'unavailable',
} as const;

export interface NetworkStatus {
  id: string;
  name: string;
  chainId: number;
  status: NetworkStatusStatus;
  blockNumber: number;
  gasGwei: number;
  blockTimeMs: number;
  pools: number;
  lastBlockAt: string;
}

export interface ScannerToken {
  symbol: string;
  name: string;
  address: string;
  decimals: number;
  chains: string[];
  liquidityUsd: number;
  pools: number;
  priceUsd: number;
  change24h: number;
}

/**
 * Live, on-chain-verified gas balance for the owner of a deployed ArbExecutor.
 */
export interface ScannerFunding {
  chainId: number;
  chain: string;
  nativeSymbol: string;
  /** Owner read from the ArbExecutor contract; native gas must be sent here. */
  operatorAddress: string;
  executorAddress: string;
  /** Current operator native-token balance in wei. */
  balanceWei: string;
  /** Safety minimum below which live execution readiness fails. */
  minimumBalanceWei: string;
  /** Exact amount needed to reach the safety minimum. */
  shortfallWei: string;
  /** Suggested top-up to reach two times the safety minimum and leave a gas buffer. */
  recommendedDepositWei: string;
  ready: boolean;
  /** Whether the ArbExecutor is currently paused. */
  paused: boolean;
  explorerUrl: string;
  checkedAt: string;
}

export type AcrossStatusExecutionMode = typeof AcrossStatusExecutionMode[keyof typeof AcrossStatusExecutionMode];


export const AcrossStatusExecutionMode = {
  'quote-only': 'quote-only',
} as const;

export type AcrossStatusBlocker = typeof AcrossStatusBlocker[keyof typeof AcrossStatusBlocker];


export const AcrossStatusBlocker = {
  'cross-chain-inventory-required': 'cross-chain-inventory-required',
} as const;

export interface AcrossStatus {
  enabled: boolean;
  apiBaseUrl: string;
  apiKeyConfigured: boolean;
  integratorIdConfigured: boolean;
  allowedChainIds: number[];
  executionMode: AcrossStatusExecutionMode;
  blocker: AcrossStatusBlocker;
}

export interface AcrossTransaction {
  to: string;
  data: string;
  value?: string;
  gas?: string;
}

export type AcrossFeesDetails = { [key: string]: unknown };

export interface AcrossFees {
  totalUsd?: number;
  totalRaw?: string;
  details?: AcrossFeesDetails;
}

export type AcrossQuoteExecutionMode = typeof AcrossQuoteExecutionMode[keyof typeof AcrossQuoteExecutionMode];


export const AcrossQuoteExecutionMode = {
  'quote-only': 'quote-only',
} as const;

export type AcrossQuoteBlocker = typeof AcrossQuoteBlocker[keyof typeof AcrossQuoteBlocker];


export const AcrossQuoteBlocker = {
  'cross-chain-inventory-required': 'cross-chain-inventory-required',
} as const;

export interface AcrossQuote {
  id?: string;
  originChainId: number;
  destinationChainId: number;
  inputToken: string;
  outputToken: string;
  amount: string;
  expectedOutputAmount: string;
  minOutputAmount?: string;
  expectedFillTimeSeconds?: number;
  quoteExpiryTimestamp?: number;
  fees: AcrossFees;
  simulationSuccess?: boolean;
  checks?: unknown;
  approvalTxns: AcrossTransaction[];
  swapTx: AcrossTransaction;
  executionMode: AcrossQuoteExecutionMode;
  executable: false;
  blocker: AcrossQuoteBlocker;
}

export type AcrossProfitBlocker = typeof AcrossProfitBlocker[keyof typeof AcrossProfitBlocker];


export const AcrossProfitBlocker = {
  'cross-chain-inventory-required': 'cross-chain-inventory-required',
} as const;

export interface AcrossProfit {
  grossSpreadUsd: number;
  totalCostsUsd: number;
  netProfitUsd: number;
  executable: false;
  blocker: AcrossProfitBlocker;
}

export interface DEXVenue {
  name: string;
  /** Raw DexScreener venue identifier (e.g. "uniswap", "pancakeswap", "velodrome", "aerodrome"), used to resolve a protocol-specific on-chain adapter for execution. */
  dexId: string;
  chain: string;
  priceUsd: number;
  liquidityUsd: number;
  feeBps: number;
  pairAddress: string;
  dexUrl: string;
  /** Raw DexScreener venue labels (e.g. ["v4"]). Absent/empty for standard V2/V3-style pools. */
  labels?: string[];
  /** Contract address of the pool's non-arbitraged (quote) token. Two venues can only be routed against each other in a single flash-loan round trip if this matches on both sides. */
  quoteTokenAddress?: string;
  quoteTokenSymbol?: string;
}

/**
 * One executable swap in an atomic arbitrage cycle, in execution order.
 */
export interface SwapRouteLeg {
  tokenInAddress: string;
  tokenInSymbol: string;
  tokenInDecimals: number;
  tokenOutAddress: string;
  tokenOutSymbol: string;
  tokenOutDecimals: number;
  venue: DEXVenue;
}

export type ProfitEstimateConfidence = typeof ProfitEstimateConfidence[keyof typeof ProfitEstimateConfidence];


export const ProfitEstimateConfidence = {
  high: 'high',
  medium: 'medium',
  low: 'low',
} as const;

export interface ProfitEstimate {
  grossProfitUsd: number;
  flashLoanFeeUsd: number;
  gasCostUsd: number;
  dexFeesUsd: number;
  slippageUsd: number;
  netProfitUsd: number;
  recommendedBorrowUsd: number;
  confidence: ProfitEstimateConfidence;
}

/**
 * Shape of the complete same-chain atomic cycle.
 */
export type ArbitrageOpportunityRouteKind = typeof ArbitrageOpportunityRouteKind[keyof typeof ArbitrageOpportunityRouteKind];


export const ArbitrageOpportunityRouteKind = {
  'two-pool': 'two-pool',
  'cross-stable': 'cross-stable',
  triangular: 'triangular',
  'multi-hop': 'multi-hop',
} as const;

/**
 * Whether profit is only graph-estimated, fully quoted on-chain, or could not be quoted by a verified adapter.
 */
export type ArbitrageOpportunityQuoteStatus = typeof ArbitrageOpportunityQuoteStatus[keyof typeof ArbitrageOpportunityQuoteStatus];


export const ArbitrageOpportunityQuoteStatus = {
  estimated: 'estimated',
  quoted: 'quoted',
  unavailable: 'unavailable',
} as const;

/**
 * Concrete reason the route cannot currently be submitted. Omitted when no blocker remains.
 */
export type ArbitrageOpportunityExecutionBlocker = typeof ArbitrageOpportunityExecutionBlocker[keyof typeof ArbitrageOpportunityExecutionBlocker];


export const ArbitrageOpportunityExecutionBlocker = {
  'negative-net': 'negative-net',
  'executor-not-deployed': 'executor-not-deployed',
  'quote-budget': 'quote-budget',
  'quote-failed': 'quote-failed',
  'target-not-allowed': 'target-not-allowed',
  'unsupported-or-open-route': 'unsupported-or-open-route',
} as const;

export type ArbitrageOpportunityStatus = typeof ArbitrageOpportunityStatus[keyof typeof ArbitrageOpportunityStatus];


export const ArbitrageOpportunityStatus = {
  new: 'new',
  monitoring: 'monitoring',
  stale: 'stale',
} as const;

export interface ArbitrageOpportunity {
  id: string;
  token: string;
  /** ERC-20 contract address of the arbitraged token on `chainId`. */
  tokenAddress: string;
  /** Decimals of the ERC-20 at `tokenAddress`, needed to convert USD amounts to on-chain wei. */
  tokenDecimals: number;
  pair: string;
  chain: string;
  chainId: number;
  spreadBps: number;
  spreadPct: number;
  buyVenue: DEXVenue;
  sellVenue: DEXVenue;
  /** Shape of the complete same-chain atomic cycle. */
  routeKind?: ArbitrageOpportunityRouteKind;
  /**
     * Explicit ordered swap path of up to six atomic swaps. Present for graph-discovered multi-protocol cycles.
     * @maxItems 6
     */
  routeLegs?: SwapRouteLeg[];
  profit: ProfitEstimate;
  detectedAt: string;
  blockNumber: number;
  executable: boolean;
  /** Whether this chain currently has a configured ArbExecutor deployment. False means exact-profit monitoring only. */
  executorDeployed?: boolean;
  /** Whether profit is only graph-estimated, fully quoted on-chain, or could not be quoted by a verified adapter. */
  quoteStatus?: ArbitrageOpportunityQuoteStatus;
  /** Concrete reason the route cannot currently be submitted. Omitted when no blocker remains. */
  executionBlocker?: ArbitrageOpportunityExecutionBlocker;
  status: ArbitrageOpportunityStatus;
}

export interface LiquidationAssetAmount {
  symbol: string;
  address: string;
  /** Raw on-chain amount, as a decimal string (may exceed safe integer precision). */
  amount: string;
  amountUsd: number;
}

/**
 * Cheap, always-on execution readiness for this position — computed for every item in the list. Deeper checks (DEX route, gas cost, competitors) are NOT included here since they cost several extra RPC/API calls per position; fetch those on demand from /liquidations/strategy when a user opens a position's detail view.
 */
export interface LiquidationStrategy {
  /** Whether an ArbExecutor contract exists on this chain at all. If false, nothing else here matters — there is no contract to send a liquidation transaction from yet. */
  executorDeployed: boolean;
  /** Real available liquidity for the debt asset in Aave's own pool right now (aToken supply minus outstanding debt) — this is what a flash loan can actually draw from. Null if it couldn't be read. */
  aaveLiquidityAvailableUsd: number | null;
  /** Whether aaveLiquidityAvailableUsd covers maxDebtToCoverUsd. Null if liquidity couldn't be read. */
  aaveLiquiditySufficient: boolean | null;
}

export interface LiquidationOpportunity {
  id: string;
  chain: string;
  chainId: number;
  /** Address of the at-risk Aave borrower. */
  userAddress: string;
  /** Which Aave V3 market this position is in (e.g. "main", "lido", "etherfi") — a chain can have several isolated markets, each with its own Pool contract, so this plus poolAddress is what a liquidator actually needs to target the right one. */
  market: string;
  /** The Aave V3 Pool contract address for this position's market — required to call liquidationCall against the correct market, since chainId alone is ambiguous once a chain has more than one market. */
  poolAddress: string;
  /** Live on-chain Health Factor. At or below 1.0 means the position is liquidatable right now. */
  healthFactor: number;
  debt: LiquidationAssetAmount;
  collateral: LiquidationAssetAmount;
  /** Protocol-defined bonus (e.g. 5 for 5%) a liquidator receives on this collateral asset. */
  liquidationBonusPct: number;
  /** Largest single-call liquidation size allowed right now (Aave's close factor — 50% normally, 100% if Health Factor is below 0.95). */
  maxDebtToCoverUsd: number;
  /** Approximate liquidator profit in USD if maxDebtToCoverUsd were liquidated now, before gas. */
  estimatedBonusUsd: number;
  detectedAt: string;
  /** True when healthFactor <= 1.0. */
  liquidatable: boolean;
  strategy: LiquidationStrategy;
}

/**
 * A real liquidator address that has actually executed LiquidationCall transactions against this specific market in the lookback window, most active first.
 */
export interface LiquidationCompetitor {
  liquidator: string;
  /** Number of liquidations this address executed on this market in the lookback window. */
  count: number;
}

/**
 * Real, verified deep execution readiness for one specific position — not a theoretical estimate. Every field is either read live on-chain/API or explicitly null when we couldn't check it, never guessed.
 */
export interface LiquidationStrategyDetail {
  /** Whether a real swap route (Uniswap V3 / Sushiswap V3 or V2 / Camelot V2) could be built for this exact trade size right now. Null if the check itself failed. */
  routeBuildable: boolean | null;
  /** Why the route isn't buildable, when routeBuildable is false (e.g. no pool on any supported venue has enough depth for this trade size). */
  routeBlockedReason: string | null;
  /** Real gas cost estimate in USD — live eth_estimateGas if an ArbExecutor is deployed here, otherwise the average of real recent historical liquidations on this exact market, priced at the current gas price. Null when not computed. */
  estimatedGasCostUsd: number | null;
  /** How estimatedGasCostUsd was derived, e.g. "live simulation" or "30d historical average (N liquidations)". */
  gasCostBasis: string | null;
  /** estimatedBonusUsd minus estimatedGasCostUsd, only meaningful when both are known. */
  estimatedNetProfitUsd: number | null;
  /** Real liquidators who have executed LiquidationCall transactions on this exact market in the last 30 days, most active first — who you're actually racing against here. */
  topCompetitors: LiquidationCompetitor[];
  /** How many historical LiquidationCall events on this market this detail was derived from (0 means no history was found — a market that has never seen a liquidation, or one just deployed). */
  sampleCount: number;
}

export type GetScannerAcrossQuoteParams = {
originChainId: number;
destinationChainId: number;
inputToken: string;
outputToken: string;
amount: string;
depositor: string;
recipient?: string;
tradeType?: GetScannerAcrossQuoteTradeType;
};

export type GetScannerAcrossQuoteTradeType = typeof GetScannerAcrossQuoteTradeType[keyof typeof GetScannerAcrossQuoteTradeType];


export const GetScannerAcrossQuoteTradeType = {
  exactInput: 'exactInput',
  minOutput: 'minOutput',
} as const;

export type GetScannerAcrossProfitParams = {
originSaleUsd: number;
destinationBuyUsd: number;
acrossFeeUsd: number;
originGasUsd: number;
destinationGasUsd: number;
slippageUsd: number;
inventoryCarryUsd?: number;
};

export type GetScannerOpportunitiesParams = {
chain?: GetScannerOpportunitiesChain;
token?: string;
minProfitBps?: number;
/**
 * @minimum 1
 * @maximum 300
 */
limit?: number;
};

export type GetScannerOpportunitiesChain = typeof GetScannerOpportunitiesChain[keyof typeof GetScannerOpportunitiesChain];


export const GetScannerOpportunitiesChain = {
  all: 'all',
  ethereum: 'ethereum',
  arbitrum: 'arbitrum',
  optimism: 'optimism',
  polygon: 'polygon',
  base: 'base',
  avalanche: 'avalanche',
  bsc: 'bsc',
  celo: 'celo',
  linea: 'linea',
  mantle: 'mantle',
  scroll: 'scroll',
  sonic: 'sonic',
  zksync: 'zksync',
  soneium: 'soneium',
} as const;

export type GetLiquidationOpportunitiesParams = {
chain?: GetLiquidationOpportunitiesChain;
/**
 * Upper bound on Health Factor to include (1.0 = exactly liquidatable).
 */
maxHealthFactor?: number;
/**
 * @minimum 1
 * @maximum 100
 */
limit?: number;
};

export type GetLiquidationOpportunitiesChain = typeof GetLiquidationOpportunitiesChain[keyof typeof GetLiquidationOpportunitiesChain];


export const GetLiquidationOpportunitiesChain = {
  all: 'all',
  arbitrum: 'arbitrum',
  ethereum: 'ethereum',
} as const;

export type GetLiquidationStrategyDetailParams = {
chainId: number;
poolAddress: string;
debtAssetAddress: string;
collateralAssetAddress: string;
/**
 * Raw on-chain amount of the debt asset to cover (LiquidationOpportunity.debt.amount).
 */
debtAmount: string;
/**
 * Raw on-chain amount of collateral expected to be seized (LiquidationOpportunity.collateral.amount).
 */
collateralAmount: string;
estimatedBonusUsd: number;
};

